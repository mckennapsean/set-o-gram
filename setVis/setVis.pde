// by Sean McKenna
// sample set vis (set'o'gram)

int w = 800;
int h = 600;

// initial setup
void setup(){
  size(w, h, P2D);
}

// begin draw cycle
void draw(){
  // wipe background each time
  background(255, 255, 255);
}
